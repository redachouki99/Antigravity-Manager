# Required: domain name pointing to this server public IP
DOMAIN=api.example.com

# Required for ACME notifications
LETSENCRYPT_EMAIL=admin@example.com

# Required: change before production use
API_KEY=replace-with-strong-api-key
WEB_PASSWORD=replace-with-strong-web-password

# Optional
LOG_LEVEL=info
